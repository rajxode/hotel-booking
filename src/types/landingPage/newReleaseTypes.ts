
import { StaticImageData } from "next/image";

export interface CardPropsType {
    img: StaticImageData;
    title: string;
    content: string;
}

export interface CardDataType {
    img: StaticImageData;
    title: string;
    content: string; 
}